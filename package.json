{
  "name": "matha",
  "version": "0.0.0",
  "private": true,
  "description": "數A 特訓 PWA — static checks and regression tests",
  "scripts": {
    "test": "node --check sw.js && node scripts/check-redirect.js"
  },
  "engines": {
    "node": ">=20"
  }
}
